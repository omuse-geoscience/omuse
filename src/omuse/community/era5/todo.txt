todo:

- add units to era5.py (strings), then in interface wip
- maintenance on cache (do not exceed maxsize, cleanup oldest)
- prefetching (using a timestep) cache
- how to handle accumulating data?
- needs timestep 
- listing of (compatible) variables
- make remote running interface / parameters
- 3D data
- add name+request to netcdf meta data..done
- select area ..done
- and maybe multiple timeslices (now data per day)/ multiple variables per file?
 time interpolation: None, nearest, linear interpolate, for nearest:
 something like:
   def hour_rounder(t):
   # Rounds to nearest hour by adding a timedelta hour if minute >= 30
     return (t.replace(second=0, microsecond=0, minute=0, hour=t.hour)
               +timedelta(hours=t.minute//30))

